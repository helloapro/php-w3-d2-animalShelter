<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <link href="/css/bootstrap.min.css" rel="stylesheet" type="text/css">
        <link href="/css/styles.css" rel="stylesheet" type="text/css">
        <script src="/js/jquery-3.1.0.min.js" type="text/javascript"></script>
        <script src="/js/bootstrap.min.js" type="text/javascript"></script>
        <script src="/js/scripts.js" type="text/javascript"></script>        <title></title>
    </head>
    <body>
        <div class="container">
            <h1>Save the Animals!</h1>
            <h3>Species of Small Furry Animals</h3>
            {% if categories is not empty %}
                <ul class="list-group">
                    {% for category in categories %}
                        <li class="list-group-item"><a href="/animals/{{ category.getId }}">{{ category.getName }}</a></li>
                    {% endfor %}
                </ul>
            {% else %}
              <p>There are no categories yet!</p>
            {% endif %}

            <form action="/categories" method="post">
                <label for="category_name">Category name:</label>
                <input id="category_name" name="category_name" type="text" class="form-control">

                <button type="submit" class="btn">Add category</button>
            </form>

        </div>
    </body>
</html>
